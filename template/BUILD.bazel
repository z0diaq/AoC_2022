"""AoC Template Project"""

load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "template",
    srcs = glob(["*.cpp", "*.ixx"]),
    copts = ["-std=c++20"],
    data = glob(["*.txt"]),
    deps = [
        "//AoC:aoc_lib",
        "@gtest//:gtest",
    ],
)
