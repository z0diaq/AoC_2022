"""Day 20: Grove Positioning System"""

load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "day20",
    srcs = glob(["*.cpp", "*.ixx"]),
    copts = ["-std=c++20"],
    data = glob(["*.txt"], allow_empty = True),
    deps = ["//AoC:aoc_lib"],
)
